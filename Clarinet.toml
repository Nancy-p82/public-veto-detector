[project]
name = "public-veto-detector"
authors = ["Public Veto Team"]
description = "A decentralized protocol for transparent and community-driven decision tracking"
telemetry = false
requirements = []
[contracts.public-veto-core]
path = "contracts/public-veto-core.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
